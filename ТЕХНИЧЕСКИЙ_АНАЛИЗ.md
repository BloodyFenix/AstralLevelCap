# Технический анализ класса PerkLevelManager

## 📋 Все использования числа 30 и LEVEL_CAP:

### В классе PerkLevelManager:

| Строка | Код | Описание | Тип |
|--------|-----|----------|-----|
| 33 | `private static int LEVEL_CAP = 30;` | Объявление переменной | **ПРЯМОЕ** использование 30 |
| 44 | `for (int i = 1; i <= LEVEL_CAP; i++)` | Цикл создания таблицы опыта | Использование переменной |
| 90 | `if (level >= LEVEL_CAP)` | Проверка максимального уровня | **ПРЯМОЕ** использование 30 |
| 102 | `return LEVEL_CAP;` | Возврат из getLevelCapFor() | Использование переменной |
| 108 | `return LEVEL_CAP;` | Возврат из resolveLevelCap() | Использование переменной |
| 114 | `return LEVEL_CAP;` | Возврат из resolveLevelCap() | Использование переменной |
| 122 | `return LEVEL_CAP;` | Возврат из resolveLevelCap() | Использование переменной |
| 129 | `LEVEL_CAP = cfg.getInt(...)` | Загрузка из конфига | Присваивание переменной |

### В других классах:

| Файл | Строка | Код | Описание |
|------|--------|-----|----------|
| PlayerProgress.java | 493 | `PerkLevelManager.getLevelCapFor(player)` | Вызов метода (использует LEVEL_CAP) |

## 🎯 Какие числа 30 нужно заменить в байткоде:

### ✅ ОБЯЗАТЕЛЬНО заменить:

1. **Строка 33**: `private static int LEVEL_CAP = 30;`
   - Байткод: `BIPUSH 30` или `SIPUSH 30`
   - Это инициализация статической переменной
   - **КРИТИЧНО**: Если не заменить, все методы будут использовать 30

2. **Строка 90**: `if (level >= LEVEL_CAP)`
   - Байткод: `BIPUSH 30` или `SIPUSH 30`
   - Это прямое сравнение с числом 30
   - **КРИТИЧНО**: Если не заменить, прогресс остановится на 30 уровне

### ℹ️ Автоматически изменятся:

Все остальные использования `LEVEL_CAP` (строки 44, 102, 108, 114, 122) автоматически будут использовать новое значение, потому что они читают значение из переменной `LEVEL_CAP`.

## 🔧 Наш подход:

### Текущая реализация (ПРАВИЛЬНАЯ):

```java
private void transformAstral(ClassNode clazz) {
    // Перебираем ВСЕ методы в классе
    for (MethodNode method : clazz.methods) {
        // Перебираем все инструкции в методе
        for (AbstractInsnNode instruction : method.instructions.toArray()) {
            // Проверяем, является ли инструкция числом 30
            if (isMaxLevelNode(instruction)) {
                // Заменяем на новое значение
                method.instructions.set(instruction, newInstruction);
            }
        }
    }
}
```

### Что это делает:

1. **Сканирует ВСЕ методы** класса (не только `ensureLevels`)
2. **Находит ВСЕ инструкции** `BIPUSH 30` или `SIPUSH 30`
3. **Заменяет их** на наше значение (100)

### Какие инструкции будут найдены и заменены:

| Метод | Строка | Что заменяется | Зачем |
|-------|--------|----------------|-------|
| `<clinit>` (статический инициализатор) | 33 | `BIPUSH 30` → `BIPUSH 100` | Инициализация LEVEL_CAP |
| `getNextLevelPercent` | 90 | `BIPUSH 30` → `BIPUSH 100` | Проверка максимального уровня |

## ✅ Вывод:

Наш мод **ПРАВИЛЬНО** реализован и заменяет **ВСЕ необходимые** вхождения числа 30:

1. ✅ Инициализацию переменной `LEVEL_CAP = 30` → `LEVEL_CAP = 100`
2. ✅ Прямую проверку `if (level >= 30)` → `if (level >= 100)`
3. ✅ Все остальные использования автоматически получат новое значение через переменную

## 🚀 Почему это работает:

1. **ASM трансформация** происходит ДО загрузки класса в память
2. **Байткод изменяется** на низком уровне (инструкции JVM)
3. **Все ссылки** на переменную `LEVEL_CAP` автоматически используют новое значение
4. **Не зависит** от конфигурации Astral Sorcery

## 📝 Дополнительные проверки:

### Проверка в других классах:

Класс `PlayerProgress.java` использует:
```java
PerkLevelManager.getLevelCapFor(player)
```

Этот метод возвращает значение `LEVEL_CAP`, которое мы уже изменили, поэтому дополнительных изменений не требуется.

### Проверка GameStages:

Если используется мод GameStages, метод `resolveLevelCap()` может вернуть другое значение, но в качестве fallback всё равно используется `LEVEL_CAP`, который мы изменили.

## 🎯 Итоговая оценка:

**Мод реализован ПРАВИЛЬНО и ПОЛНОСТЬЮ.**

Все необходимые изменения выполняются, дополнительных модификаций не требуется.
